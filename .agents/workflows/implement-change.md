# Workflow: Implement Change

1. Follow the approved `IMPLEMENTATION_PLAN.md`.
2. Write clean, documented code and corresponding tests in `src/` and `tests/`.
3. Maintain working progress in `CHANGELOG_WORKING.md`.
4. Conduct peer/agent code review and secure coding checks.
5. Finalize `BUILD_HANDOFF.json`.